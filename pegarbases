require(foreign)
require (questionr)
setwd("/Users/SALA-C17/Desktop")
sdem<- data.frame(read.dbf("sdemt216.dbf"))
coe<- data.frame(read.dbf("coe1t216.dbf"))
llave <- paste(sdem$CD_A, sdem$ENT,sdem$CON, sdem$V_SEL, sdem$N_HOG, sdem$H_MUD, sdem$N_REN)
new <- data.frame(sdem, llave)
newer <- data.frame(coe, llave)
total <- merge(new,newer, by "llave", all.x = TRUE)
